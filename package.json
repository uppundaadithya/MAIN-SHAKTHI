{
  "name": "main-shakthi",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "back"
  ],
  "scripts": {
    "start": "npm --prefix back start",
    "dev": "npm --prefix back run dev",
    "build": "npm --prefix back run build",
    "install:back": "npm --prefix back install"
  }
}
